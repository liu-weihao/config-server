server:
  port: 20480
eureka:
  client:
    service-url:
      defaultZone: http://peer2:8761/eureka
zuul:
  routes:
    user:
      path: /user/**
      serviceId: taxi-user
    auth:
      path: /auth/**
      serviceId: auth-center
