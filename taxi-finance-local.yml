server:
  port: 8103
spring:
  application:
    name: taxi-finance
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/taxi-account?useUnicode=true&characterEncoding=UTF8
    username: root
    password: 123456
    # 使用Hikari数据源
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
eureka:
  client:
    service-url:
      defaultZone: http://peer2:8762/eureka
